---
name: ui-fast-prototyper
description: "Rapid UI component generation with Tailwind CSS v4+, accessibility, responsive design, and dark mode support."
triggers: crea componente, fai la pagina, stile tailwind, UI component, frontend, React component
---
# UI Fast Prototyper

## When to Use
- "Crea componente", "fai la pagina", "stile tailwind"
- Rapid UI prototyping

## Protocol

### 1. Styling
- Use **Tailwind CSS v4+** exclusively
- Mobile-first responsive design
- Dark mode via `dark:` variant

### 2. Accessibility Requirements
- All interactive elements have `aria-label` or `aria-labelledby`
- Proper heading hierarchy (h1 → h6)
- Color contrast ratio ≥ 4.5:1 (WCAG AA)
- Keyboard navigation support
- Focus visible states

### 3. Component Structure
- Fully typed props interface
- Default prop values
- Clear separation of concerns

### 4. Output
- Complete component code
- Import instructions
- Props documentation table
- Usage example
